{
  "T01": {"owner": "haiku-agent", "status": "done_repaired", "output": "tasks/T01-variants.json", "note": "3/7 SHAs fabricated by the low-effort model, corrected by main agent via git log -1 verification"},
  "T02": {"owner": "sonnet-agent", "status": "done", "output": "tasks/T02-canonical-task.md"},
  "T03": {"owner": "main", "status": "partial", "output": "contract.md#hardware-contract", "remaining": "confirm maturin/cargo/mpi module build actually succeeds on a genoa allocation; lscpu SMT check"},
  "T04": {"owner": "main", "status": "done", "depends_on": ["contract.md frozen"], "deliverable": "opt-in Rust gate trace in propagate loops + bindings + tests", "output": "GateTrace (unpartitioned+direct path), PartitionLayerRecord.{circuit_index,application_index,gate_name,nanos} (partitioned+MPI, shared run_layers), PropagationStats/PartitionStats getters (circuit_index, application_index, gate_name, nanos); verified: cargo test --workspace, clippy -D warnings, cargo test --features mpi, scripts/mpi-test.sh --ranks 2,4, pytest (451 passed). Caught+fixed a real regression: the direct/small-sum prefix path recorded no gate trace at all (engine_selection auto/direct silently truncated the trace to the sorted-suffix length only), covered by a new small_sum_path.rs assertion."},
  "T05": {"owner": "main", "status": "done", "depends_on": ["contract.md frozen"], "deliverable": "Julia wrapper extended to canonical task + timing schema", "output": "No Julia/wrapper code changes needed: verified (not modified). benchmarks/python/test_julia_parity.py 32/32 pass on this host/revision, gate vocabulary (rx, pauli_rotation ZZ, cnot) already covers heavy_hex_kicked_ising. Live pilot of the real canonical circuit (theta_h=7pi/32, 5 steps, eps=2^-6) via bench_c_deep_trotter.py: 1355/1355 per-layer term counts identical between paulistrings and PauliPropagation.jl, |delta<O>|=0. T04's new PropagationStats.nanos/gate_name/circuit_index already flow through the existing propagate_with_stats call with zero wrapper changes. Julia's per-gate wall-time gap (runner.jl has no equivalent to GateTrace) is pre-existing, already noted in runner.jl's own output notes, and accepted per the handoff's own fallback rule -- not fabricated."},
  "T06": {"owner": "subagent", "status": "done", "depends_on": ["contract.md frozen"], "deliverable": "schema validation + normalization + plot-table generation, synthetic fixtures only", "output": "quera-talk-data/campaign-2026-09-11/analysis/{schema,normalize,validate_campaign}.py + tests/; 12/12 pytest, independently re-verified by main agent. Known gap: hash_communication() is an empty-list placeholder -- run-record schema has no partition_row_policy field yet."},
  "T07": {"owner": "subagent", "status": "done", "depends_on": ["contract.md", "T03"], "deliverable": "resumable campaign driver + Slurm scripts (prepared, not submitted) + preflight", "output": "quera-talk-data/campaign-2026-09-11/jobs/{preflight,run_cell}.py + campaign-genoa.sbatch + job-ledger.{jsonl,md} + tests/; 17/17 pytest and preflight.py's honest failure report both independently re-verified by main agent on this (non-genoa) host. Known gaps: genoa CPU fingerprint is MEDIUM confidence (secondary sources, cannot distinguish Genoa from Bergamo -- needs confirming against a real genoa allocation's /proc/cpuinfo before trusting hardware_valid=true); PropagationStats has no per-gate support_weight/bucket_bits/partner_count (Rust-CLI-only fields), written null; setup/scatter/gather_time_s null (not separately isolated). Deferred: C4 multi-node/MPI stage, E8 hash-comparison, E1/E2 historical-revision worktree builds (non-bucketed_current variants write status=skipped_variant)."},
  "T08": {"owner": "main", "status": "done", "depends_on": ["T04", "T05", "T06", "T07"], "deliverable": "timing-semantics review findings", "output": "Reviewed timing-boundary correctness (want_timer spans prepare through finalize incl. exchange on the partitioned/MPI path, matches contract), no new collectives added by tracing, direct-path regression test specifically covers the application_index/circuit_index continuity across the direct-to-sorted transition. No correctness issues found beyond the two gaps already logged under T06/T07."},
  "T09": {"owner": "subagent", "status": "done", "depends_on": ["T06"], "deliverable": "recurring 2-panel figure stages + scaling/hash/accuracy figures", "output": "quera-talk-data/campaign-2026-09-11/figures/{make_recurring_figure,make_compact_figures}.py + tests/; 13/13 pytest independently re-verified. All 7 synthetic stage PNGs render only under figures/_synth/ (gitignored, confirmed via git status --ignored), sources untracked. Known caveats: efficiency_gates/binned rows need caller-side enrichment with variant_id/threads/ranks (joined by run_id); hash_communication figure raises NotImplementedError pending partition_row_policy (same gap as T06); stage->variant mapping for stage 4 picked by elimination, flagged, worth a human check once real data exists."},
  "T10": {"owner": "main", "status": "done_pending_real_data", "depends_on": ["accepted data", "T09"], "deliverable": "evidence.md E0-E9 mapping, headline table, reproduce.sh", "output": "campaign.json, README.md, evidence.md, reproduce.sh all written. evidence.md honestly marks every E0-E9 row tooling-ready/blocked with its exact prerequisite (no invented results). reproduce.sh's non-cluster stages (test-tooling: 42/42 pass, preflight, submit-c1c2/c3 print-only) verified working; tables/figures stages intentionally exit 2 with an explanation since no real data exists to wire them to yet. Headline table is empty pending real runs -- correctly so."
}
