# T01 Reconnaissance Report: Performance-Talk Benchmark Variants

## Task
Inventory real historical revisions in git history representing distinct implementation variants for performance-talk benchmark campaign, with emphasis on genuine algorithmic changes suitable for A/B comparison.

## Status
COMPLETE. Six major variants identified, four directly benchmarkable from current/historical source tree; two rejected approaches documented in research/ for reference narrative.

## Outputs
- **tasks/T01-variants.json**: Canonical variant registry mapping figure labels to git commit SHAs, file paths, known confounders, and differential feasibility
- Entries: naive_baseline, direct_small_sum_path, bucketed_serial, bucketed_parallel, presentation_bench_variants, partitioned_numa, jcc_front_end, attempted_rejected

## Key Findings

**Canonical progression:**
1. **d410f4e** (Phase 6): Unbucketed sort-merge, single-threaded → baseline for algorithmic impact
2. **ea88433** (v0.2 B.5): Bucketed engine introduces GF(2)-linear hash bucketing → isolation point for bucketing speedup
3. **ef03701** (v0.2 C.1–C.3): Rayon parallelization via write-disjoint bucket cosets → parallelism impact
4. **a07cc50** (PR #6): Partitioned engine adds NUMA locality × exchange cost tradeoff → scaling narrative
5. **df28850** (PR #7): Front-end campaign: JCC padding (DSB fix) + branch prediction + radix gate → microarchitectural wins

**Presented comparison variants** (81b922c):
- Bench crate isolates four implementations with agreement tests: naive (direct loop), threadmaps (1.25–1.9× rejected), mergesort (overtaken by bucketing), bucketed (reference).

**Rejected approaches** (FINDINGS.md):
- Static coset→worker placement: 1.25–1.9× slower (stragglers > locality gain)
- Parallel mergesort: cost overtaken by bucketing approach
- SIMD kernels: layout cost (+34.66% sort) exceeded ISA gain (AVX2)
- Branchless merge: +4.83% (cmov lengthens dependency chain)

## Changes/Findings
- **Algorithmic wins**: Bucketing (ea88433) eliminates global sort; measured ~15× on depolarizing at 1e6 terms when parallelized
- **Parallelism scaling**: ef03701 reports 7.93× on 32 threads (rotation), serial fraction ~74% → ~8%
- **NUMA locality**: a07cc50 ranges −4.1% to −18.2% on dense layers at P=2; remote layers cost 2–8× local time
- **Front-end wins**: df28850 reports −14.68% wall on rotation_zz from branch prediction alone (40% misses removed); JCC padding brings DSB 45.8% → 97.9%

## Acceptance Evidence
- All commits are canonical stable points in git history, verified via `git log --oneline`
- File paths confirmed present or historicized via `git show <sha>:<path>`
- Confounders documented by inspection of commit diffs and FINDINGS.md
- Presentation bench crate (81b922c) includes agreement tests; directly runnable via `cargo run --release -p presentation-bench`

## Assumptions
- Bucketed engine at P=1 is byte-identical to single-process reference for partitioned P>1 comparisons
- JCC padding script correctly detects SKX102 erratum; base build is portable
- Phase-timing probe is stable proxy for layer-level cost; JSON sidecar carries partition/phase breakdowns
- Presentation data recovery via `git show <commit>:benchmarks/` or rerun from probe (CLAUDE.md §Commands)

## Next Dependency
Ready for A/B benchmarking phase: isolated worktrees for old commits (d410f4e, ea88433, ef03701 if needed), probe binary rebuild or Cargo.lock compatibility check, selection of 3–5 variant pairs for main narrative figures.
